{
    "name": "dk-balor/churnex",
    "description": "Churnex - AI-Powered Churn Prediction Platform",
    "type": "project",
    "require": {
        "php": ">=8.0"
    },
    "scripts": {
        "post-install-cmd": [
            "npm install",
            "npm run build"
        ],
        "post-update-cmd": [
            "npm install",
            "npm run build"
        ]
    }
} 